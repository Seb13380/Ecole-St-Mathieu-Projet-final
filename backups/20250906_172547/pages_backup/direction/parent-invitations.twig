{% extends "layouts/base.twig" %}

{% block title %}
	<title>Demandes d'inscription - École Saint-Mathieu</title>
{% endblock %}

{% block main %}
	<div class="min-h-screen bg-gray-50 py-8">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<div class="mb-8">
				<h1 class="text-3xl font-bold text-gray-900">
					📝 Gestion des demandes d'inscription
				</h1>
				<p class="mt-2 text-gray-600">
					Validez ou refusez les demandes d'inscription des parents
				</p>
			</div>

			<!-- Statistiques -->
			<div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
				<div class="bg-white p-4 rounded-lg shadow">
					<div class="text-2xl font-bold text-yellow-600">{{ stats.pending }}</div>
					<div class="text-sm text-gray-600">En attente</div>
				</div>
				<div class="bg-white p-4 rounded-lg shadow">
					<div class="text-2xl font-bold text-green-600">{{ stats.approved }}</div>
					<div class="text-sm text-gray-600">Approuvées</div>
				</div>
				<div class="bg-white p-4 rounded-lg shadow">
					<div class="text-2xl font-bold text-red-600">{{ stats.rejected }}</div>
					<div class="text-sm text-gray-600">Refusées</div>
				</div>
				<div class="bg-white p-4 rounded-lg shadow">
					<div class="text-2xl font-bold text-blue-600">{{ stats.total }}</div>
					<div class="text-sm text-gray-600">Total</div>
				</div>
			</div>

			<!-- Liste des demandes -->
			<div class="bg-white shadow rounded-lg">
				<div class="px-6 py-4 border-b border-gray-200">
					<h2 class="text-lg font-medium text-gray-900">
						📋 Demandes en attente
					</h2>
				</div>
				<div class="overflow-x-auto">
					<table class="min-w-full divide-y divide-gray-200">
						<thead class="bg-gray-50">
							<tr>
								<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
									Parent
								</th>
								<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
									Enfant
								</th>
								<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
									Contact
								</th>
								<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
									Date demande
								</th>
								<th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
									Actions
								</th>
							</tr>
						</thead>
						<tbody class="bg-white divide-y divide-gray-200">
							{% for request in requests %}
								<tr>
									<td class="px-6 py-4 whitespace-nowrap">
										<div class="text-sm font-medium text-gray-900">
											{{ request.parentFirstName }}
											{{ request.parentLastName }}
										</div>
									</td>
									<td class="px-6 py-4 whitespace-nowrap">
										<div class="text-sm text-gray-900">
											{{ request.childFirstName }}
											{{ request.childLastName }}
										</div>
										<div class="text-sm text-gray-500">
											Né(e) le
											{{ request.childBirthDate|date('d/m/Y') }}
										</div>
									</td>
									<td class="px-6 py-4 whitespace-nowrap">
										<div class="text-sm text-gray-900">{{ request.parentEmail }}</div>
										<div class="text-sm text-gray-500">{{ request.parentPhone }}</div>
									</td>
									<td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
										{{ request.createdAt|date('d/m/Y H:i') }}
									</td>
									<td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
										<div class="flex space-x-2">
											<button onclick="approveRequest({{ request.id }})" class="text-green-600 hover:text-green-900">
												✅ Approuver
											</button>
											<button onclick="rejectRequest({{ request.id }})" class="text-red-600 hover:text-red-900">
												❌ Refuser
											</button>
											<button onclick="viewDetails({{ request.id }})" class="text-blue-600 hover:text-blue-900">
												👁️ Détails
											</button>
										</div>
									</td>
								</tr>
							{% else %}
								<tr>
									<td colspan="5" class="px-6 py-12 text-center text-gray-500">
										Aucune demande en attente
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>

	<script>
		// Approuver une demande
async function approveRequest(id) {
if (!confirm('Approuver cette demande d\'inscription ?')) 
return;


try {
const response = await fetch (`/inscriptions/${id}/approve`, {method: 'POST'});

const result = await response.json();

if (result.success) {
alert('✅ Demande approuvée et compte parent créé');
location.reload();
} else {
alert('❌ ' + result.error);
}
} catch (error) {
alert('❌ Erreur lors de l\'approbation');
console.error(error);
}
}

// Refuser une demande
async function rejectRequest(id) {
const reason = prompt('Motif du refus (optionnel) :');
if (reason === null) 
return;


try {
const response = await fetch (`/inscriptions/${id}/reject`, {
method: 'POST',
headers: {
'Content-Type': 'application/json'
},
body: JSON.stringify({reason})
});

const result = await response.json();

if (result.success) {
alert('✅ Demande refusée');
location.reload();
} else {
alert('❌ ' + result.error);
}
} catch (error) {
alert('❌ Erreur lors du refus');
console.error(error);
}
}

// Voir les détails
function viewDetails(id) {
window.location.href = `/inscriptions/${id}/details`;
}
	</script>
{% endblock %}
