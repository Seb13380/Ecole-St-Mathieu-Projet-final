{% extends "../layouts/base.twig" %}

{% block main %}
	<div class="min-h-screen bg-gradient-to-br from-blue-50 to-green-50 py-8">
		<div
			class="container mx-auto px-4">

			<!-- Header -->
			<div class="text-center mb-12">
				<h1 class="text-4xl font-bold text-gray-800 mb-4">Images et Vidéos</h1>
				<p class="text-lg text-gray-600">Découvrez les moments forts de notre école</p>

				{% if user and (user.role == 'ADMIN' or user.role == 'DIRECTION' or user.role == 'GESTIONNAIRE_SITE') %}
					<div class="mt-6">
						<a href="/gallery/admin" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg transition-colors">
							🔧 Gérer la galerie
						</a>
					</div>
				{% endif %}
			</div>

			<!-- Galerie par thèmes -->
			{% if themes|length > 0 %}
				{% for theme in themes %}
					{% if theme.medias|length > 0 %}
						<div class="mb-16">
							<div
								class="bg-white rounded-xl shadow-lg overflow-hidden">

								<!-- En-tête du thème -->
								<div class="bg-gradient-to-r from-blue-600 to-green-600 text-white p-6">
									<h2 class="text-2xl font-bold mb-2">{{ theme.name }}</h2>
									{% if theme.description %}
										<p class="text-blue-100">{{ theme.description }}</p>
									{% endif %}
									<div class="text-sm mt-2 opacity-75">
										{{ theme.medias|length }}
										{% if theme.medias|length > 1 %}éléments{% else %}élément
										{% endif %}
									</div>
								</div>

								<!-- Grille des médias -->
								<div class="p-6">
									<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
										{% for media in theme.medias %}
											<div class="bg-gray-50 rounded-lg overflow-hidden shadow-md hover:shadow-lg transition-shadow cursor-pointer media-item" onclick="openModal('{{ media.id }}', '{{ media.type }}', '{{ media.filename }}', '{{ media.title }}', '{{ media.description }}')">

												{% if media.type == 'IMAGE' %}
													<div class="aspect-square bg-gray-200 relative">
														<img src="/uploads/gallery/{{ media.filename }}" alt="{{ media.title }}" class="w-full h-full object-cover">
														<div class="absolute top-2 right-2">
															<span class="bg-black bg-opacity-50 text-white text-xs px-2 py-1 rounded">📷</span>
														</div>
													</div>
												{% else %}
													<div class="aspect-square bg-gray-800 relative flex items-center justify-center">
														<div class="text-white text-center">
															<div class="text-4xl mb-2">🎬</div>
															<div class="text-sm">Vidéo</div>
														</div>
														<div class="absolute top-2 right-2">
															<span class="bg-black bg-opacity-50 text-white text-xs px-2 py-1 rounded">🎥</span>
														</div>
													</div>
												{% endif %}

												<div class="p-4">
													<h3 class="font-semibold text-gray-800 truncate">{{ media.title }}</h3>
													{% if media.description %}
														<p class="text-sm text-gray-600 mt-1 line-clamp-2">{{ media.description }}</p>
													{% endif %}
													<div class="text-xs text-gray-500 mt-2">
														{{ media.createdAt|date('d/m/Y') }}
													</div>
												</div>
											</div>
										{% endfor %}
									</div>
								</div>
							</div>
						</div>
					{% endif %}
				{% endfor %}
			{% else %}
				<div class="text-center py-16">
					<div class="text-6xl mb-4">📸</div>
					<h2 class="text-2xl font-bold text-gray-600 mb-2">Aucune image pour le moment</h2>
					<p class="text-gray-500">La galerie sera bientôt remplie de beaux souvenirs !</p>
				</div>
			{% endif %}
		</div>
	</div>

	<!-- Modal pour afficher les médias -->
	<div id="mediaModal" class="fixed inset-0 bg-black bg-opacity-75 hidden z-50 flex items-center justify-center">
		<div class="bg-white rounded-lg max-w-4xl max-h-screen m-4 overflow-hidden">
			<div class="flex justify-between items-center p-4 border-b">
				<h3 id="modalTitle" class="text-xl font-bold"></h3>
				<button onclick="closeModal()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
			</div>

			<div class="p-4 max-h-96 overflow-y-auto">
				<div
					id="modalContent" class="text-center"><!-- Le contenu sera injecté ici -->
				</div>
				<div id="modalDescription" class="mt-4 text-gray-600"></div>
			</div>
		</div>
	</div>

	<style>
		.line-clamp-2 {
			display: -webkit-box;
			-webkit-line-clamp: 2;
			-webkit-box-orient: vertical;
			overflow: hidden;
		}

		.media-item:hover {
			transform: translateY(-2px);
			transition: transform 0.2s ease;
		}

		.aspect-square {
			aspect-ratio: 1 / 1;
		}
	</style>

	<script>
		function openModal(id, type, filename, title, description) {
const modal = document.getElementById('mediaModal');
const modalTitle = document.getElementById('modalTitle');
const modalContent = document.getElementById('modalContent');
const modalDescription = document.getElementById('modalDescription');

modalTitle.textContent = title;
modalDescription.textContent = description || '';

if (type === 'IMAGE') {
modalContent.innerHTML = `<img src="/uploads/gallery/${filename}" alt="${title}" class="max-w-full max-h-96 object-contain">`;
} else {
modalContent.innerHTML = `
      <video controls class="max-w-full max-h-96">
        <source src="/uploads/gallery/${filename}" type="video/mp4">
        Votre navigateur ne supporte pas la lecture vidéo.
      </video>
    `;
} modal.classList.remove('hidden');
document.body.style.overflow = 'hidden';
}

function closeModal() {
const modal = document.getElementById('mediaModal');
modal.classList.add('hidden');
document.body.style.overflow = 'auto';

// Arrêter la vidéo si elle joue
const video = modal.querySelector('video');
if (video) {
video.pause();
}
}

// Fermer la modal avec Escape
document.addEventListener('keydown', function (e) {
if (e.key === 'Escape') {
closeModal();
}
});

// Fermer la modal en cliquant en dehors
document.getElementById('mediaModal').addEventListener('click', function (e) {
if (e.target === this) {
closeModal();
}
});
	</script>

{% endblock %}
