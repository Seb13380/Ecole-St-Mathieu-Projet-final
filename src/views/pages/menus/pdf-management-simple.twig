{% extends "layouts/base.twig" %}

{% block title %}Gestion des Menus PDF - École Saint-Mathieu
{% endblock %}

{% block main %}
	<div class="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50 p-8">
		<div
			class="max-w-7xl mx-auto">
			<!-- Header Section -->
			<div class="text-center mb-8">
				<h1 class="text-4xl font-bold text-slate-800 mb-4">
					<span class="mr-3">🍽️</span>
					Gestion des Menus PDF
				</h1>
				<p class="text-slate-600">Gérez les menus hebdomadaires envoyés par la société Dupond</p>
			</div>

			<!-- Messages -->
			{% if success %}
				<div class="max-w-4xl mx-auto mb-6">
					<div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
						<div class="flex items-center">
							<svg class="w-5 h-5 mr-2" fill="currentColor" viewbox="0 0 20 20">
								<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
							</svg>
							{{ success }}
						</div>
					</div>
				</div>
			{% endif %}

			{% if error %}
				<div class="max-w-4xl mx-auto mb-6">
					<div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
						<div class="flex items-center">
							<svg class="w-5 h-5 mr-2" fill="currentColor" viewbox="0 0 20 20">
								<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
							</svg>
							{{ error }}
						</div>
					</div>
				</div>
			{% endif %}

			<!-- Upload Section -->
			<div class="mb-8">
				<div class="bg-white shadow-lg border-l-4 border-orange-500 p-8 max-w-4xl mx-auto">
					<h2 class="text-2xl font-bold text-slate-800 mb-6 text-center">
						<span class="mr-2">📤</span>
						Ajouter un Nouveau Menu
					</h2>

					<form action="/admin/menus-pdf/create" method="POST" enctype="multipart/form-data" class="space-y-6">
						<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
							<div>
								<label for="semaine" class="block text-sm font-medium text-slate-700 mb-2">
									Titre du menu
								</label>
								<input type="text" id="semaine" name="semaine" class="w-full px-4 py-3 border border-slate-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition-colors" placeholder="Menu du 18 au 22 août 2025" required>
							</div>

							<div>
								<label for="description" class="block text-sm font-medium text-slate-700 mb-2">
									Description (optionnel)
								</label>
								<input type="text" id="description" name="description" class="w-full px-4 py-3 border border-slate-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition-colors" placeholder="Menu spécial rentrée">
							</div>
						</div>

						<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
							<div>
								<label for="dateDebut" class="block text-sm font-medium text-slate-700 mb-2">
									Date de début
								</label>
								<input type="date" id="dateDebut" name="dateDebut" class="w-full px-4 py-3 border border-slate-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition-colors" required>
							</div>

							<div>
								<label for="dateFin" class="block text-sm font-medium text-slate-700 mb-2">
									Date de fin
								</label>
								<input type="date" id="dateFin" name="dateFin" class="w-full px-4 py-3 border border-slate-300 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 transition-colors" required>
							</div>
						</div>

						<div>
							<label for="menuPdf" class="block text-sm font-medium text-slate-700 mb-2">
								Fichier PDF du menu
								<span class="text-red-500">*</span>
							</label>
							<div class="border-2 border-dashed border-slate-300 hover:border-orange-500 transition-colors p-8 text-center">
								<input type="file" id="menuPdf" name="menuPdf" accept=".pdf" required class="hidden" onchange="updateFileName(this)">
								<label for="menuPdf" class="cursor-pointer">
									<div class="text-6xl mb-4">📄</div>
									<div class="text-lg font-medium text-slate-700 mb-2">Cliquez pour sélectionner le PDF</div>
									<div class="text-sm text-slate-500">Fichier PDF maximum 10MB</div>
									<div id="fileName" class="text-sm font-medium text-orange-600 mt-2 hidden"></div>
								</label>
							</div>
						</div>

						<div class="flex items-center">
							<input type="checkbox" id="actif" name="actif" class="h-4 w-4 text-orange-600 focus:ring-orange-500 border-gray-300 rounded">
							<label for="actif" class="ml-2 block text-sm text-gray-900">
								Activer ce menu immédiatement
							</label>
						</div>

						<div class="text-center">
							<button type="submit" class="bg-orange-500 hover:bg-orange-600 text-white font-bold py-4 px-8 transition-colors inline-flex items-center text-lg">
								<svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
								</svg>
								Ajouter le Menu
							</button>
						</div>
					</form>
				</div>
			</div>

			<!-- Menus existants -->
			<div class="mb-8">
				<h2 class="text-2xl font-bold text-slate-800 mb-6 text-center">
					<span class="mr-2">📋</span>
					Menus Existants ({{ menus|length }})
				</h2>

				{% if menus and menus|length > 0 %}
					<div class="grid gap-6 max-w-6xl mx-auto">
						{% for menu in menus %}
							<div class="bg-white shadow-md border-l-4 {{ menu.actif ? 'border-green-500' : 'border-slate-300' }} p-6 hover:shadow-lg transition-all duration-300">
								<div class="flex flex-col md:flex-row md:items-center justify-between">
									<div class="flex-1">
										<div class="flex items-center mb-3">
											<h3 class="text-xl font-bold text-slate-800 mr-4">{{ menu.semaine }}</h3>
											{% if menu.actif %}
												<span class="bg-green-100 text-green-800 px-3 py-1 text-xs font-medium border border-green-200">
													🟢 MENU ACTIF
												</span>
											{% else %}
												<span class="bg-slate-100 text-slate-600 px-3 py-1 text-xs font-medium border border-slate-200">
													📋 Planifié
												</span>
											{% endif %}
										</div>

										{% if menu.description %}
											<p class="text-slate-600 mb-2">{{ menu.description }}</p>
										{% endif %}

										<div class="text-sm text-slate-500 space-y-1">
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
												</svg>
												Du
												{{ menu.dateDebut|date('d/m/Y') }}
												au
												{{ menu.dateFin|date('d/m/Y') }}
											</div>
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
												</svg>
												Ajouté par
												{{ menu.auteur.firstName }}
												{{ menu.auteur.lastName }}
											</div>
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
												</svg>
												{{ menu.dateCreation|date('d/m/Y à H:i') }}
											</div>
										</div>
									</div>

									<div
										class="flex flex-col sm:flex-row gap-3 mt-4 md:mt-0 md:ml-6">
										<!-- Bouton Voir PDF -->
										<a href="{{ menu.pdfUrl }}" target="_blank" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 text-sm font-medium transition-colors inline-flex items-center justify-center">
											<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
											</svg>
											Voir PDF
										</a>

										<!-- Bouton Activer/Désactiver -->
										{% if not menu.actif %}
											<form action="/admin/menus-pdf/{{ menu.id }}/activate" method="POST" class="inline">
												<button type="submit" class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 text-sm font-medium transition-colors inline-flex items-center justify-center w-full sm:w-auto">
													<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
														<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
													</svg>
													Activer
												</button>
											</form>
										{% else %}
											<form action="/admin/menus-pdf/{{ menu.id }}/toggle" method="POST" class="inline">
												<button type="submit" class="bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 text-sm font-medium transition-colors inline-flex items-center justify-center w-full sm:w-auto">
													<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
														<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2"></path>
													</svg>
													Désactiver
												</button>
											</form>
										{% endif %}

										<!-- Bouton Supprimer -->
										<button onclick="confirmDelete({{ menu.id }}, '{{ menu.titre }}')" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 text-sm font-medium transition-colors inline-flex items-center justify-center">
											<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
											</svg>
											Supprimer
										</button>
									</div>
								</div>
							</div>
						{% endfor %}
					</div>
				{% else %}
					<div class="text-center py-12">
						<div class="bg-white shadow-md p-8 max-w-2xl mx-auto">
							<div class="text-6xl mb-4">📄</div>
							<h3 class="text-xl font-bold text-slate-800 mb-2">Aucun menu disponible</h3>
							<p class="text-slate-600">Ajoutez votre premier menu PDF pour commencer.</p>
						</div>
					</div>
				{% endif %}
			</div>

			<!-- Information Section -->
			<div class="bg-blue-50 border border-blue-200 p-6 max-w-4xl mx-auto">
				<h3 class="text-lg font-bold text-blue-900 mb-3">
					<span class="mr-2">ℹ️</span>
					Information importante
				</h3>
				<div class="text-blue-800 text-sm space-y-2">
					<p>• Vous pouvez ajouter et activer plusieurs menus simultanément.</p>
					<p>• Tous les menus marqués comme "ACTIF" seront visibles sur la page des menus.</p>
					<p>• Les fichiers PDF doivent faire maximum 10MB et être au format PDF uniquement.</p>
					<p>• Vous pouvez activer ou désactiver les menus à tout moment.</p>
				</div>
			</div>
		</div>
	</div>

	<!-- Modal de confirmation de suppression -->
	<div id="deleteModal" class="fixed inset-0 bg-black bg-opacity-50 hidden z-50 flex items-center justify-center">
		<div class="bg-white p-6 max-w-md mx-4 shadow-xl">
			<h3 class="text-lg font-bold text-slate-800 mb-4">Confirmer la suppression</h3>
			<p class="text-slate-600 mb-6">Êtes-vous sûr de vouloir supprimer le menu "<span id="menuTitle"></span>" ? Cette action est irréversible.</p>
			<div class="flex justify-end space-x-4">
				<button onclick="closeDeleteModal()" class="bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 transition-colors">
					Annuler
				</button>
				<form id="deleteForm" method="POST" class="inline">
					<button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 transition-colors">
						Supprimer
					</button>
				</form>
			</div>
		</div>
	</div>
{% endblock %}

{% block scripts %}
	{{ parent() }}
	<script>
		function updateFileName(input) {
const fileName = document.getElementById('fileName');
if (input.files && input.files[0]) {
fileName.textContent = '📄 ' + input.files[0].name;
fileName.classList.remove('hidden');
} else {
fileName.classList.add('hidden');
}
}

function confirmDelete(menuId, menuTitle) {
document.getElementById('menuTitle').textContent = menuTitle;
document.getElementById('deleteForm').action = `/admin/menus-pdf/${menuId}?_method=DELETE`;
document.getElementById('deleteModal').classList.remove('hidden');
}
function closeDeleteModal() {
document.getElementById('deleteModal').classList.add('hidden');
}

// Fermer la modal en cliquant à l'extérieur
document.getElementById('deleteModal').addEventListener('click', function (e) {
if (e.target === this) {
closeDeleteModal();
}
});

// Auto-remplir les dates avec la semaine courante
document.addEventListener('DOMContentLoaded', function () {
const today = new Date();
const monday = new Date(today);
monday.setDate(today.getDate() - today.getDay() + 1);

const friday = new Date(monday);
friday.setDate(monday.getDate() + 4);

document.getElementById('dateDebut').value = monday.toISOString().split('T')[0];
document.getElementById('dateFin').value = friday.toISOString().split('T')[0];
});
	</script>
{% endblock %}
