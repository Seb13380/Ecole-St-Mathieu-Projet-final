<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Test Gestion Actualités</title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body class="bg-gray-100">
		<div class="container mx-auto px-4 py-8">
			<h1 class="text-3xl font-bold mb-6">📰 Gestion des Actualités - TEST</h1>

			<div class="bg-white p-6 rounded-lg shadow">
				<h2 class="text-xl font-semibold mb-4">Actualités trouvées :
					{{ actualites|length }}</h2>

				{% if actualites|length > 0 %}
					<div class="grid gap-4">
						{% for actualite in actualites %}
							<div class="border p-4 rounded">
								<h3 class="font-semibold">{{ actualite.titre }}</h3>
								<p class="text-gray-600 text-sm">
									Par
									{{ actualite.auteur.firstName }}
									{{ actualite.auteur.lastName }}
									({{ actualite.auteur.role }})
								</p>
								<p class="text-gray-500 text-xs">{{ actualite.datePublication|date('d/m/Y H:i') }}</p>

								{% if actualite.important %}
									<span class="bg-red-100 text-red-800 text-xs px-2 py-1 rounded">Important</span>
								{% endif %}

								{% if actualite.visible %}
									<span class="bg-green-100 text-green-800 text-xs px-2 py-1 rounded">Publié</span>
								{% else %}
									<span class="bg-yellow-100 text-yellow-800 text-xs px-2 py-1 rounded">Brouillon</span>
								{% endif %}
							</div>
						{% endfor %}
					</div>
				{% else %}
					<p class="text-gray-500">Aucune actualité trouvée.</p>
				{% endif %}

				<div class="mt-6 p-4 bg-blue-50 rounded">
					<h3 class="font-semibold">Informations de debug :</h3>
					<p>
						<strong>Titre page :</strong>
						{{ title }}</p>
					{% if user %}
						<p>
							<strong>Utilisateur :</strong>
							{{ user.firstName }}
							{{ user.lastName }}
							({{ user.role }})</p>
					{% else %}
						<p>
							<strong>Utilisateur :</strong>
							Non défini</p>
					{% endif %}
				</div>
			</div>
		</div>
	</body>
</html>
