{% extends "layouts/base.twig" %}

{% block title %}Gestion du Carrousel Principal - École Saint-Mathieu
{% endblock %}

{% block main %}
	<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 p-8">
		<div
			class="max-w-7xl mx-auto">
			<!-- Header Section -->
			<div class="text-center mb-8">
				<h1 class="text-4xl font-bold text-slate-800 mb-4">
					<span class="mr-3">🖼️</span>
					Gestion du Carrousel Principal
				</h1>
				<p class="text-slate-600">Gérez les images du grand carrousel de la page d'accueil (900px de hauteur)</p>
			</div>

			<!-- Messages -->
			{% if success %}
				<div class="max-w-4xl mx-auto mb-6">
					<div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
						<div class="flex items-center">
							<svg class="w-5 h-5 mr-2" fill="currentColor" viewbox="0 0 20 20">
								<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
							</svg>
							{{ success }}
						</div>
					</div>
				</div>
			{% endif %}

			{% if error %}
				<div class="max-w-4xl mx-auto mb-6">
					<div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
						<div class="flex items-center">
							<svg class="w-5 h-5 mr-2" fill="currentColor" viewbox="0 0 20 20">
								<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
							</svg>
							{{ error }}
						</div>
					</div>
				</div>
			{% endif %}

			<!-- Statistiques -->
			<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8 max-w-4xl mx-auto">
				<div class="bg-white p-6 rounded-lg shadow-md border-l-4 border-blue-500">
					<div class="flex items-center">
						<div class="flex-shrink-0">
							<div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center">
								<span class="text-blue-600 font-bold">📊</span>
							</div>
						</div>
						<div class="ml-4">
							<p class="text-sm font-medium text-gray-500">Total Images</p>
							<p class="text-2xl font-bold text-gray-900">{{ stats.total }}</p>
						</div>
					</div>
				</div>

				<div class="bg-white p-6 rounded-lg shadow-md border-l-4 border-green-500">
					<div class="flex items-center">
						<div class="flex-shrink-0">
							<div class="w-8 h-8 bg-green-100 rounded-lg flex items-center justify-center">
								<span class="text-green-600 font-bold">✅</span>
							</div>
						</div>
						<div class="ml-4">
							<p class="text-sm font-medium text-gray-500">Images Actives</p>
							<p class="text-2xl font-bold text-gray-900">{{ stats.active }}</p>
						</div>
					</div>
				</div>

				<div class="bg-white p-6 rounded-lg shadow-md border-l-4 border-gray-500">
					<div class="flex items-center">
						<div class="flex-shrink-0">
							<div class="w-8 h-8 bg-gray-100 rounded-lg flex items-center justify-center">
								<span class="text-gray-600 font-bold">⏸️</span>
							</div>
						</div>
						<div class="ml-4">
							<p class="text-sm font-medium text-gray-500">Images Inactives</p>
							<p class="text-2xl font-bold text-gray-900">{{ stats.inactive }}</p>
						</div>
					</div>
				</div>
			</div>

			<!-- Ajout d'une nouvelle image -->
			<div class="mb-8">
				<div class="bg-white shadow-lg border-l-4 border-green-500 p-8 max-w-4xl mx-auto rounded-lg">
					<h2 class="text-2xl font-bold text-slate-800 mb-6 text-center">
						<span class="mr-2">➕</span>
						Ajouter une Nouvelle Image
					</h2>

					<form action="/hero-carousel/add" method="POST" enctype="multipart/form-data" class="space-y-6">
						<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
							<div class="md:col-span-2">
								<label for="image" class="block text-sm font-medium text-slate-700 mb-2">
									Image
									<span class="text-red-500">*</span>
								</label>
								<input type="file" id="image" name="image" accept="image/*" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors" required>
								<p class="text-xs text-slate-500 mt-1">Formats acceptés: JPEG, PNG, WebP (max 10MB)</p>
							</div>

							<div>
								<label for="titre" class="block text-sm font-medium text-slate-700 mb-2">
									Titre (optionnel)
								</label>
								<input type="text" id="titre" name="titre" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors" placeholder="Ex: Rentrée 2025">
							</div>

							<div>
								<label for="ordre" class="block text-sm font-medium text-slate-700 mb-2">
									Ordre d'affichage
								</label>
								<input type="number" id="ordre" name="ordre" value="0" min="0" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors">
								<p class="text-xs text-slate-500 mt-1">0 = en premier</p>
							</div>

							<div class="md:col-span-2">
								<label for="description" class="block text-sm font-medium text-slate-700 mb-2">
									Description (optionnelle)
								</label>
								<textarea id="description" name="description" rows="3" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors" placeholder="Description de l'image..."></textarea>
							</div>
						</div>

						<div class="text-center">
							<button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-4 px-8 rounded-lg transition-colors inline-flex items-center text-lg">
								<svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
								</svg>
								Ajouter l'Image
							</button>
						</div>
					</form>
				</div>
			</div>

			<!-- Liste des images existantes -->
			<div class="mb-8">
				<h2 class="text-2xl font-bold text-slate-800 mb-6 text-center">
					<span class="mr-2">🖼️</span>
					Images du Carrousel Principal ({{ images|length }})
				</h2>

				{% if images and images|length > 0 %}
					<div class="grid gap-6 max-w-6xl mx-auto">
						{% for image in images %}
							<div id="image-{{ image.id }}" class="bg-white shadow-md border-l-4 {{ image.active ? 'border-green-500' : 'border-gray-300' }} p-6 hover:shadow-lg transition-all duration-300 rounded-lg">
								<div
									class="flex flex-col md:flex-row gap-6">
									<!-- Image preview -->
									<div class="flex-shrink-0">
										<img src="{{ image.originalUrl }}" alt="{{ image.titre|default('Image carousel') }}" class="w-48 h-32 object-cover rounded-lg shadow-md">
									</div>

									<!-- Informations -->
									<div class="flex-1">
										<div class="flex items-center mb-3 flex-wrap gap-2">
											<h3 class="text-xl font-bold text-slate-800">
												{{ image.titre|default('Image sans titre') }}
											</h3>

											{% if image.active %}
												<span class="bg-green-100 text-green-800 px-3 py-1 text-xs font-medium rounded-full border border-green-200">
													✅ ACTIVE
												</span>
											{% else %}
												<span class="bg-gray-100 text-gray-600 px-3 py-1 text-xs font-medium rounded-full border border-gray-200">
													⏸️ INACTIVE
												</span>
											{% endif %}

											<span class="bg-blue-100 text-blue-800 px-3 py-1 text-xs font-medium rounded-full border border-blue-200">
												📋 Ordre:
												{{ image.ordre }}
											</span>
										</div>

										{% if image.description %}
											<p class="text-slate-600 mb-4 leading-relaxed">{{ image.description }}</p>
										{% endif %}

										<div class="text-sm text-slate-500 space-y-1">
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
												</svg>
												Ajouté par
												{{ image.auteur.firstName }}
												{{ image.auteur.lastName }}
											</div>
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
												</svg>
												Ajouté le
												{{ image.createdAt|date('d/m/Y à H:i') }}
											</div>
											<div class="flex items-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
												</svg>
												Fichier:
												{{ image.filename }}
											</div>
										</div>
									</div>

									<!-- Boutons d'action -->
									<div class="flex-shrink-0">
										<div
											class="flex flex-col gap-2">
											<!-- Bouton Modifier -->
											<button onclick="editImage({{ image.id }}, '{{ (image.titre|default(''))|e('js') }}', '{{ (image.description|default(''))|e('js') }}', {{ image.ordre }}, {{ image.active ? 1 : 0 }})" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors inline-flex items-center justify-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
												</svg>
												Modifier
											</button>

											<!-- Bouton Statut -->
											<form action="/hero-carousel/{{ image.id }}/toggle" method="POST" class="inline">
												<button type="submit" class="w-full {% if image.active %}bg-yellow-500 hover:bg-yellow-600{% else %}bg-green-500 hover:bg-green-600{% endif %} text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors inline-flex items-center justify-center">
													{% if image.active %}
														<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
															<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
														</svg>
														Désactiver
													{% else %}
														<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
															<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.828 14.828a4 4 0 01-5.656 0M9 10h1m4 0h1m-6 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
														</svg>
														Activer
													{% endif %}
												</button>
											</form>

											<!-- Bouton Supprimer -->
											<button onclick="confirmDelete({{ image.id }}, '{{ (image.titre|default('cette image'))|e('js') }}')" class="w-full bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors inline-flex items-center justify-center">
												<svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewbox="0 0 24 24">
													<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
												</svg>
												Supprimer
											</button>
										</div>
									</div>
								</div>
							</div>
						{% endfor %}
					</div>
				{% else %}
					<div class="text-center py-12">
						<div class="bg-white shadow-md rounded-lg p-8 max-w-2xl mx-auto">
							<div class="text-6xl mb-4">🖼️</div>
							<h3 class="text-xl font-bold text-slate-800 mb-2">Aucune image dans le carrousel principal</h3>
							<p class="text-slate-600">Ajoutez vos premières images pour le grand carrousel de la page d'accueil.</p>
						</div>
					</div>
				{% endif %}
			</div>

			<!-- Information Section -->
			<div class="bg-blue-50 border border-blue-200 rounded-lg p-6 max-w-4xl mx-auto">
				<h3 class="text-lg font-bold text-blue-900 mb-3">
					<span class="mr-2">ℹ️</span>
					Informations importantes
				</h3>
				<div class="text-blue-800 text-sm space-y-2">
					<p>• Les images actives s'affichent automatiquement dans le grand carrousel de la page d'accueil.</p>
					<p>• L'ordre d'affichage détermine la séquence des images (0 = première image).</p>
					<p>• Formats recommandés: JPEG ou PNG, taille optimale: 1920x900px pour le grand carrousel.</p>
					<p>• Vous pouvez désactiver temporairement une image sans la supprimer.</p>
					<p>• Les images prises avec vos téléphones seront automatiquement redimensionnées.</p>
				</div>
			</div>
		</div>
	</div>

	<!-- Modal de modification -->
	<div id="editModal" class="fixed inset-0 bg-black bg-opacity-60 hidden z-50 flex items-center justify-center">
		<div class="bg-white rounded-xl p-8 max-w-2xl mx-4 shadow-2xl max-h-screen overflow-y-auto border-t-4 border-blue-500">
			<div class="flex items-center mb-6">
				<div class="bg-blue-100 rounded-full p-3 mr-4">
					<svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewbox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
					</svg>
				</div>
				<h3 class="text-2xl font-bold text-slate-800">Modifier l'image</h3>
			</div>
			<form id="editForm" method="POST">
				<div class="space-y-6">
					<div>
						<label for="editTitre" class="block text-sm font-medium text-slate-700 mb-2">Titre</label>
						<input type="text" id="editTitre" name="titre" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors">
					</div>
					<div>
						<label for="editDescription" class="block text-sm font-medium text-slate-700 mb-2">Description</label>
						<textarea id="editDescription" name="description" rows="4" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors"></textarea>
					</div>
					<div>
						<label for="editOrdre" class="block text-sm font-medium text-slate-700 mb-2">Ordre d'affichage</label>
						<input type="number" id="editOrdre" name="ordre" min="0" class="w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors">
					</div>
					<div class="flex items-center">
						<input type="checkbox" id="editActive" name="active" class="w-4 h-4 text-blue-600 bg-slate-100 border-slate-300 focus:ring-blue-500 focus:ring-2 rounded">
						<label for="editActive" class="ml-2 text-sm font-medium text-slate-700">Image active</label>
					</div>
				</div>
				<div class="flex justify-end space-x-4 mt-8 pt-6 border-t border-slate-200">
					<button type="button" onclick="closeEditModal()" class="bg-slate-500 hover:bg-slate-600 text-white px-6 py-3 rounded-lg transition-colors font-medium">
						Annuler
					</button>
					<button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg transition-colors font-medium">
						💾 Sauvegarder
					</button>
				</div>
			</form>
		</div>
	</div>

	<!-- Modal de confirmation de suppression -->
	<div id="deleteModal" class="fixed inset-0 bg-black bg-opacity-60 hidden z-50 flex items-center justify-center">
		<div class="bg-white rounded-xl p-8 max-w-md mx-4 shadow-2xl border-t-4 border-red-500">
			<div class="flex items-center mb-6">
				<div class="bg-red-100 rounded-full p-3 mr-4">
					<svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewbox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
					</svg>
				</div>
				<h3 class="text-2xl font-bold text-red-700">Confirmer la suppression</h3>
			</div>
			<div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
				<p class="text-red-800">
					⚠️ Êtes-vous sûr de vouloir supprimer l'image
					<span class="font-bold">"<span id="imageTitle"></span>"</span>
					?
				</p>
				<p class="text-red-600 text-sm mt-2">Cette action est irréversible et supprimera définitivement le fichier.</p>
			</div>
			<div class="flex justify-end space-x-4">
				<button onclick="closeDeleteModal()" class="bg-slate-500 hover:bg-slate-600 text-white px-6 py-3 rounded-lg transition-colors font-medium">
					Annuler
				</button>
				<form id="deleteForm" method="POST" class="inline">
					<button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-6 py-3 rounded-lg transition-colors font-medium">
						🗑️ Supprimer définitivement
					</button>
				</form>
			</div>
		</div>
	</div>

	<script>
		// Gestion des modales et interactions
function editImage(id, titre, description, ordre, active) {
console.log('✏️ Édition image:', {
id,
titre,
description,
ordre,
active
});

document.getElementById('editTitre').value = titre || '';
document.getElementById('editDescription').value = description || '';
document.getElementById('editOrdre').value = ordre || 0;
document.getElementById('editActive').checked = active === 1;
document.getElementById('editForm').action = `/hero-carousel/${id}`;
document.getElementById('editModal').classList.remove('hidden');
}

function closeEditModal() {
document.getElementById('editModal').classList.add('hidden');
}

function confirmDelete(imageId, imageTitle) {
console.log('🗑️ Suppression image:', {imageId, imageTitle});

document.getElementById('imageTitle').textContent = imageTitle || 'Image';
document.getElementById('deleteForm').action = `/hero-carousel/${imageId}/delete`;
document.getElementById('deleteModal').classList.remove('hidden');
}

function closeDeleteModal() {
document.getElementById('deleteModal').classList.add('hidden');
}

// Fermer les modales en cliquant à l'extérieur
document.addEventListener('DOMContentLoaded', function () {
const editModal = document.getElementById('editModal');
const deleteModal = document.getElementById('deleteModal');

editModal.addEventListener('click', function (e) {
if (e.target === this) 
closeEditModal();


});

deleteModal.addEventListener('click', function (e) {
if (e.target === this) 
closeDeleteModal();


});
});

// Scroll automatique vers l'élément ciblé
window.addEventListener('DOMContentLoaded', function () {
if (window.location.hash) {
const targetElement = document.querySelector(window.location.hash);
if (targetElement) {
setTimeout(() => {
targetElement.scrollIntoView({behavior: 'smooth', block: 'center'});
}, 100);
}
}
});
	</script>

{% endblock %}
