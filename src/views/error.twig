{% extends "layouts/main.twig" %}

{% block content %}
<div class="min-h-screen bg-gradient-to-br from-red-50 to-red-100 flex items-center justify-center py-12">
    <div class="container mx-auto px-4">
        <div class="max-w-2xl mx-auto text-center">
            <div class="bg-white rounded-xl shadow-lg p-8">
                <div class="mb-6">
                    <div class="text-6xl text-red-500 mb-4">⚠️</div>
                    <h1 class="text-3xl font-bold text-gray-800 mb-4">Une erreur est survenue</h1>
                </div>
                
                <div class="mb-6">
                    <p class="text-lg text-gray-600">
                        {% if message %}
                            {{ message }}
                        {% else %}
                            Une erreur inattendue s'est produite.
                        {% endif %}
                    </p>
                </div>
                
                <div class="space-y-4">
                    <a href="/" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition-colors duration-300">
                        🏠 Retour à l'accueil
                    </a>
                    
                    <div class="text-sm text-gray-500">
                        <p>Si le problème persiste, contactez l'administration de l'école.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
